/resume.pdf /assets/resume/Colt-Fulk-Resume-2026.pdf 200!

# Analytics ingestion — same-origin proxy to the Supabase `ingest` edge function.
# Same-origin means no CORS and, crucially, not ad-blockable (blockers commonly
# drop *.supabase.co). The function is deployed with verify_jwt=false so it's
# publicly callable without an apikey. Must sit ABOVE the SPA catch-all.
/api/collect  https://lhkvuqqeitfdztmttzax.supabase.co/functions/v1/ingest  200!

# Auth + gated content endpoints (Netlify Functions). Declared explicitly so the
# SPA catch-all below cannot shadow them.
/api/login       /.netlify/functions/login        200!
/api/logout      /.netlify/functions/logout       200!
/api/session     /.netlify/functions/session      200!
/api/case-study  /.netlify/functions/case-study   200!

/* /index.html 200
